/*
 * SimpleLineChart Messages
 *
 * This contains all the text for the SimpleLineChart component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SimpleLineChart';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SimpleLineChart component!',
  },
});
